<?php
	$boo = true;	//declare one varable which type is boolean, initialize it as true value
	if ($boo == true)//judge if it is true
		echo"variable$boois true";//if it is true, output "variable is true"
	else
		echo"variable $boo is false";//if it is false, output "varible is false"
	#attention:no only the boolean can be "false", in some special condition the other type of variables can be "false", too. These special condition is : 0, 0.0, "0", blank string(""), array only declare without initialize
?>
